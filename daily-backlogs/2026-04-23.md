# Daily Backlog — 2026-04-23

Generated 2026-04-23T00:00:00Z. Excluded 0 items already in resolved-log.md.

## Recommended for today

1. **Populate `stripe_price_id` values in `lead_databases`** — `supabase/migrations/20260228000002_add_stripe_price_id_to_lead_databases.sql:4`
   - Why: Column exists but is unpopulated per the migration's own TODO. Without real Stripe Price IDs, paid lead checkout (`useLeadCheckout`) cannot create Checkout Sessions — revenue blocker for the leads marketplace.
   - Effort: S · Impact: 5 · Risk: medium (money path)
   - Next step: run `/implement-item` with "item 1"

2. **Lock down `process-email-queue` — `verify_jwt=false` with no internal auth** — `supabase/config.toml` + `supabase/functions/process-email-queue/index.ts`
   - Why: Function is `verify_jwt = false` and uses `SUPABASE_SERVICE_ROLE_KEY` without any Bearer/admin check. Any anonymous caller can trigger the queue processor, forcing early email sends or burning Resend quota. Cron-only invocation should be enforced via a shared secret header or admin gate.
   - Effort: S · Impact: 4 · Risk: medium (abuse + deliverability reputation)
   - Next step: run `/implement-item` with "item 2"

3. **Replace mock lead preview data with real `lead_database_records`** — `src/components/leads/LeadPreviewModal.tsx:71`, `src/constants/mockPreviewData.ts:2`, `src/hooks/useLeadCheckout.ts:22`
   - Why: Three separate TODOs point at the same gap: users see fabricated preview rows before buying a lead database. Undermines trust and conversion on a paid surface.
   - Effort: M · Impact: 4 · Risk: medium (user-visible paid feature)
   - Next step: run `/implement-item` with "item 3"

## Full list

4. **Stripe webhook idempotency + structured logging** — GitHub issues #20, #29
   - Sprint 1 items that appear unresolved in the webhook handler. Dropped/duplicated events could corrupt `user_subscriptions`. Verify idempotency keys and retry tolerance.
   - Effort: M · Impact: 5 · Risk: high (billing integrity)

5. **RLS enforcement check — paid content after downgrade/cancel** — GitHub issue #30
   - Validate that users whose subscription drops from `growth`/`scale`/`enterprise` back to `free` actually lose access to tier-gated report sections and lead databases. Manual + SQL test plan.
   - Effort: M · Impact: 5 · Risk: medium (security/leakage)

6. **Angel investors — batch 02 enrichment** — commit `0758a5e` ("Enrich angel investors — batch 01 (20 records)")
   - Explicit "batch 01" naming implies a continuing series. Next 20 records with the same 4-migration split pattern.
   - Effort: M · Impact: 3 · Risk: low

7. **VC investors — import batch 2 (IDs 11+)** — commit `a269dcd` ("Import 10 VC investors … batch 1, IDs 1-10")
   - Schema columns (`currently_investing`, `leads_deals`, `fund_size`, etc.) were added in Phase 1 migration `20260407000001_add_investor_vc_columns.sql`. Only 10 rows imported so far.
   - Effort: M · Impact: 3 · Risk: low

8. **Legal links in footer + Stripe Checkout** — GitHub issue #37
   - `/privacy` and `/terms` pages exist per `CLAUDE.md` route map, but Stripe Checkout config and global footer links may not yet reference them. Compliance item for payment flow.
   - Effort: S · Impact: 2 · Risk: low

9. **Wire `ai-chat` edge function to a real model** — `supabase/functions/ai-chat/index.ts:99`, `src/hooks/useAIChat.ts:109`
   - Both sides of the chat feature are stubbed. The function already has `verify_jwt = true`, so scaffolding is in place; needs Lovable AI Gateway call (matching `generate-report` pattern) and frontend streaming.
   - Effort: L · Impact: 3 · Risk: low

10. **Backend function test coverage** — GitHub issue #33
    - No automated tests for edge functions. Highest leverage: `stripe-webhook` (idempotency, signature), `generate-report` (JWT path), `send-email` (opt-out + dedupe).
    - Effort: L · Impact: 3 · Risk: low

## Sources consulted

- Git log: 60 commits scanned over last 30 days, 3 explicit batch/phase patterns matched
- TODO/FIXME: 6 hits surfaced (4 in `src/`, 1 in edge functions, 1 in migrations)
- GitHub issues: 31 open, 5 surfaced (filtered: non-stale, actionable, scoped to this repo)
- GitHub PRs: 0 open
- Known gotchas (CLAUDE.md §12): 10 reviewed, none promoted (all documented intentional behavior)
- Schema drift: 2 tables referenced via `(supabase as any).from(...)` — `lead_databases`, `lead_database_records`. Intentional per codebase pattern; no item surfaced.
- Edge function audit: 16 functions cross-checked against `config.toml` — all present; `process-email-queue` flagged for item 2.
- Directory coverage: SKIPPED — Supabase MCP not available in this session, cannot query row counts.
